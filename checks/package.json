{
  "name": "@mcpscorecard/checks",
  "version": "0.1.0",
  "description": "Open rubric-as-code: the checks and scoring behind the MCP server security scorecard. Each check verifies a numbered section of the MCP Server Security Policy.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/j2abro/mcpscorecard.git",
    "directory": "checks"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2"
  }
}
