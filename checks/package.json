{
  "name": "@mcpscorecard/checks",
  "version": "0.1.0",
  "description": "Open scoring model: the checks and scoring behind the MCP server security scorecard. Each check verifies a numbered section of the MCP Server Security Policy.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluemotionlabs/mcpscorecard.git",
    "directory": "checks"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "scan": "tsx src/scan.ts"
  },
  "devDependencies": {
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^2"
  }
}
